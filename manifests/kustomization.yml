apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lilsecret
resources:
- namespace.yml
- lilsecret.yml
- ingress.yml
- ssl.yml
# Placeholder — deploy.sh / the workflow set the real image at apply time
# and never commit it.
images:
- name: lilsecret
  newName: lilsecret
  newTag: latest
