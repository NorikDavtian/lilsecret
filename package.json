{
  "name": "lilsecret",
  "private": true,
  "type": "module",
  "description": "Encrypted one-time secret sharing — say it once, then ash.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  }
}
